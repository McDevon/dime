function UnitType(name, hp, speed, attack, defence)
{
    this.name       = name;
    this.hp         = hp;
    this.speed      = speed;
    this.attack     = attack;
    this.defence    = defence;
}
