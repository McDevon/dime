function UnitType(name, hp, speed, attack, defence, enemy)
{
    this.name       = name;
    this.hp         = hp;
    this.speed      = speed;
    this.attack     = attack;
    this.defence    = defence;
    this.enemy      = enemy;
}
